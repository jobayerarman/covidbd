<!DOCTYPE html>
<html lang="bd">

<head>
  <% include ../partials/header.ejs %>
</head>

<body>
  <% include ../partials/nav.ejs %>

  <div class="container-fluid px-md-5 mt-4 mb-4">

    <div class="row row-cols-1 row-cols-md-2 row-cols-xl-6">
      <div class="col-sm">
        <p>
          <small><span class="update-head border border-primary pl-3 pr-2 py-2">সর্বশেষঃ </span><span
              class="update-data border border-info px-4 py-2"><%= updated %></span></small>
        </p>
      </div>
    </div>

    <div class="row row-cols-1 row-cols-md-2 row-cols-xl-6 text-center">

      <div class="col mb-2">
        <div class="card bg-warning">
          <div class="card-body text-white">
            <p>গত ২৪ ঘণ্টায় নতুন শনাক্ত</p>
            <h2 class="card-title"><%= todayCases %></h2>
          </div>
          <div class="card-footer py-1 text-white">
            <%= todayCasesRateBn %>%
            <% if (todayCasesRateEn < 0) { %>
            (কমেছে)
            <% } else { %>
            (বেড়েছে)
            <% } %>
          </div>
        </div>
      </div>

      <div class="col mb-2">
        <div class="card bg-danger">
          <div class="card-body text-white">
            <p>গত ২৪ ঘণ্টায় মোট মৃত্যু</p>
            <h2 class="card-title"><%= todayDeaths %></h2>
          </div>
          <div class="card-footer py-1 text-white">
            <%= todayDeathRateBn %>%
            <% if (todayDeathRateEn < 0) { %>
            (কমেছে)
            <% } else { %>
            (বেড়েছে)
            <% } %>
          </div>
        </div>
      </div>

      <div class="col mb-2">
        <div class="card border border-warning">
          <div class="card-body">
            <p>মোট শনাক্ত</p>
            <h2 class="card-title text-warning"><%= cases %></h2>
          </div>
          <div class="card-footer py-1">
            <%= totatCasesRateBn %>%
            <% if (totatCasesRateEn < 0) { %>
            (কমেছে)
            <% } else { %>
            (বেড়েছে)
            <% } %>
          </div>
        </div>
      </div>

      <div class="col mb-2">
        <div class="card border-danger">
          <div class="card-body">
            <p>মোট মৃত</p>
            <h2 class="card-title text-danger"><%= deaths %></h2>
          </div>
          <div class="card-footer py-1">
            <%= totalDeathsRateBn %>%
            <% if (totalDeathsRateEn < 0) { %>
            (কমেছে)
            <% } else { %>
            (বেড়েছে)
            <% } %>
          </div>
        </div>
      </div>

      <div class="col mb-2">
        <div class="card border border-success">
          <div class="card-body">
            <p>মোট সুস্থ</p>
            <h2 class="card-title text-success"><%= recovered %> </h2>
          </div>
          <div class="card-footer py-1">
            <%= recoveredRateBn %>%
            <% if (recoveredRateEn < 0) { %>
            (কমেছে)
            <% } else { %>
            (বেড়েছে)
            <% } %>
          </div>
        </div>
      </div>

      <div class="col mb-2">
        <div class="card border border-info">
          <div class="card-body">
            <p>মোট পরিক্ষাকৃত নমুনার সংখ্যা</p>
            <h2 class="card-title text-info"><%= tests %> </h2>
          </div>
          <div class="card-footer py-1">
            <%= testRateBn %>%
            <% if (testRateEn < 0) { %>
            (কমেছে)
            <% } else { %>
            (বেড়েছে)
            <% } %>
          </div>
        </div>
      </div>

    </div>
  </div>

  <div class="container-fluid px-md-5 mt-4 mb-4">
    <div class="row">
      <div class="col-lg-6 col-md-12">
        <div class="chart-container table-responsive">
          <canvas id="coronaCasesByTime" class="table" style="max-height: 450px"></canvas>
        </div>
      </div>

      <div class="col-lg-6 col-md-12">
        <div class="chart-container table-responsive">
          <canvas id="coronaDeathByTime" class="table" style="max-height: 450px"></canvas>
        </div>
      </div>
    </div>
  </div>

  <div class="container-fluid px-lg-5">
    <div class="row">
      <div class="col-lg-6 col-md-12">
        <div class="text-center mb-2">
          <h4>বিভাগভিত্তিক আক্রান্তের সংখ্যা</h4>
        </div>
        <div class="mb-5">
          <table id="datatable-one" class="table table-sm table-striped table-hover table-bordered datatable">
            <thead class="thead-light">
              <tr>
                <th>বিভাগের নাম</th>
                <th class="text-center">মোট আক্রান্ত</th>
              </tr>
            </thead>
            <tbody>
              <% divisions.forEach(function(division) { %>
              <tr>
                <td><%= division.name %></td>
                <td class="text-center"><%= division.count %></td>
              </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      </div>

      <div class="col-lg-6 col-md-12">
        <div class="text-center mb-2">
          <h4>জেলাভিত্তিক আক্রান্তের সংখ্যা</h4>
        </div>
        <table id="datatable-two" class="table table-sm table-striped table-hover table-bordered datatable">
          <thead class="thead-light">
            <tr>
              <th>জেলার নাম</th>
              <th scope="col" class="text-center">মোট আক্রান্ত</th>
            </tr>
          </thead>
          <tbody>
            <% districts.forEach(function(district) { %>
            <tr>
              <td class=""><%= district.name %></td>
              <td class="text-center"><%= district.count %></td>
            </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    </div>
  </div>

  <div class="container-fluid mt-4 mb-4 text-center">
    <div class="row">
      <div class="col-sm">
        <p class="py-2"><small>API data by <a href="https://github.com/novelcovid/api" target="_blank">Novel
              CoVID19/API</a></small></>
      </div>
    </div>
  </div>

  <% include ../partials/footer.ejs %>
</body>

</html>
